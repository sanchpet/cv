name: Render CV

on:
  push:
    paths:
      - Curriculum_Vitae.typ

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: typst-community/setup-typst@v4

      - name: Install Charter font
        run: sudo apt-get install -y fonts-urw-base35

      - name: Compile to SVG
        run: typst compile Curriculum_Vitae.typ "preview-{p}.svg"

      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: render preview SVGs"
          file_pattern: "preview-*.svg"
